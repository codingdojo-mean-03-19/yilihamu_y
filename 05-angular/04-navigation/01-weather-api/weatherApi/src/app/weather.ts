export class Weather {

// tslint:disable-next-line: max-line-length
  constructor(public temp: number, public pressure: number, public temp_max : number, public temp_min : number, public all: number) {}
}
