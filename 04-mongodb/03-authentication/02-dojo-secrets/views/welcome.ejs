<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css"
  integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous">
  <title>Welcome</title>
</head>
<body>
  <form>
    <a href="/sessions">Logout</a>
	</form>
  <div class="col-md-6">
    <h3>Dojo Secrets</h3>
    <h5>Create a secret</h5>
    <form action="/secret" method="POST">
        <div class="form-group">
          <label for="secret">Secret</label>
          <input type="text" class="form-control" name="secret">
        </div>
        <button type="submit" class="btn btn-primary">Submit</button>
    </form>
  </div>
    <div class="col-md-6">
        <h3>All the secrets</h3>
        <div class="card-body scroller">
            <% if(data) { %>
                <% for(result of data) { %>
                    <% if(result._users == user) {%>
                        <p>
                            <a href="/secret/<%= result._id %>"><%= result.secret %> </a>
                            <a href="/destroy/<%= result._id %>">Delete</a>
                        </p>
                    <% }else{ %>
                        <p><a href="/secret/<%= result._id %>"><%= result.secret %> </a></p>
                    <% } %>
                <% } %>
            <% } %>
        </div>
    </div>
</body>
</html>
